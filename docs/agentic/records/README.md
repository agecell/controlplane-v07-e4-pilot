# Work records

Store safe summaries under `<developer>/<story>/` on branch `ae/records/<developer>/<sprint>`. Add package summaries under `<developer>/<sprint>/`. Main aggregates subagent results. Never commit manifests/lease tokens, private absolute paths, raw logs, credentials, or sensitive real data. Publish code and records before handoff.
