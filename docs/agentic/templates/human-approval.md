# Note for human reviewers — v0.5

Fill from verified facts; replace placeholders. Do not publish secrets or lease tokens.

```text
Use only when project/organization policy requires a human or when DEVELOPER_REVIEW is active.
MR / candidate full SHA: <reviewed>
Reviewer identity / authority: <real human reviewer>
Decision / findings / evidence inspected: <actual>
For an additional human gate under AGENT_MERGE, the human reviewer posts a GitLab MR note:
APPROVE <FULL_CANDIDATE_SHA>
Main records the real note ID and verifies it through the helper.
This template is not approval. An agent must never fabricate human approval or impersonate a reviewer.
```
